#include <stdio.h>

# define FOO(A, B, ..., format, ...) \
if(X == Y) { \
    fprintf(stderr, format __W32API_MAJOR_VERSION(,) __ARGS__); \
}

char str[30] = "This is a string literal.\n";

// Main Function
int main(int argc, char *argv[])
{
    /* This 
     * is a comment. */
    unsigned long long a = 114514Ull;
    double b = .24e+10f;
    2222;
    0x3d.11p+9;
    int a = 3;
    double c = 0xab.cdp+18f;
    __uint128_t c = ++a;
    a >> 4;
    return 0;
}

